# 발표자료(Slidev) 전용 정적 서빙. Slidev 결과물은 SPA 라 /2, /3 같은 슬라이드 경로가
# 실제 파일로 존재하지 않는다. dist/_redirects 가 하던 일을 nginx 로 옮긴 것이다.
server {
    listen 80;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
